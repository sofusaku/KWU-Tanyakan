import React from "react";
// import styled from "styled-components";

const HomePage = props => (
    <React.Fragment>
        <div>Hello, world! Current node environment is: {process.env.NODE_ENV}</div>
    </React.Fragment>
)

export default HomePage;